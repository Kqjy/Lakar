# Normalize to LF in the repo; check out native on Windows.
* text=auto

# Files that must stay LF (used inside the Docker image)
Dockerfile text eol=lf
*.sh text eol=lf
*.yml text eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff2 binary
